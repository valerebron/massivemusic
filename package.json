{
  "name": "massivemusic2",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "front": "cd front && yarn dev",
    "back": "export DB_PASS=root && docker-compose up -d && cd back && yarn dev",
    "dev": "yarn back & yarn front",
    "seed": "cd back && yarn seed",
    "build": "cd front && vue-cli-service build && cd ../back && yarn deploy && cd ../ && docker build -t registry.osrp.xyz/root/massivemusic2 --build-arg DB_PASS=root ."
  },
  "author": "valere",
  "license": "MIT"
}
