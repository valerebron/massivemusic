{
  "name": "massivemusic",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "install": "cd back && yarn && cd ../front && yarn",
    "generate": "cd back && npx prisma generate",
    "build:front": "cd front/src && npx vue-cli-serve build",
    "build:back": "docker-compose build",
    "build": "yarn build:front && yarn build:back",
    "migrate:save": "cd back && npx prisma migrate save --experimental",
    "migrate:up": "cd back && npx prisma migrate up --experimental",
    "dev:front": "cd front/src && npx vue-cli-serve serve",
    "dev:back": "cd back && npx prisma generate --watch && npx nodemon --ext ts,graphql --watch '*' --ignore 'src/**/*.spec.ts' --exec 'ts-node' src/index.ts",
    "dev": "yarn dev:front & yarn dev:back & docker-compose up",
    "kill": "kill $(lsof -t -i:3330)"
  },
  "author": "valere",
  "license": "GPL3"
}
